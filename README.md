When using the repo script you must define what branch to sync with or it will default to master branch.
This is done for example:
jet/repo.sh sam

Where "sam" is the branch being fetched and synced with.
